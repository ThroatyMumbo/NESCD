MEMORY {
    CODE0:   start = $8000, size = $7FFA, file = %O, fill = yes;
    VECTORS: start = $FFFA, size = $0006, file = %O, fill = yes;
}

SEGMENTS {
    CODE:    load = CODE0,   type = ro;
    CODE2:   load = CODE0,   type = ro;
    TILES:   load = CODE0,   type = ro;
    VECTORS: load = VECTORS, type = ro;
}
